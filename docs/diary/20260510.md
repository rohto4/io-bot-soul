# 2026-05-10 作業記録

## 概要

今日は、カスタム絵文字の分類結果について、各 rank フォルダの件数を確認し、実運用で参照しやすい形でドキュメントへ残した。

主目的は、`1-rank-a` / `2-rank-b` / `3-rank-c` / `4_rank-d` の件数を、`illust` / `illust-text` / `text` の粒度で把握できるようにすることだった。

## 実施内容

### 集計

`data/images` 配下の分類先フォルダを確認し、次の件数を集計した。

| rank | 意味 | illust | illust-text | text |
|---|---|---:|---:|---:|
| `1-rank-a` | ノート本文にもリアクションにも使いやすい候補 | 1271 | 230 | 1110 |
| `2-rank-b` | 主にノート本文向きの候補 | 298 | 188 | 1827 |
| `3-rank-c` | 主にリアクション向きの候補 | 90 | 45 | 401 |
| `4_rank-d` | 今回の主分類対象から外したアニメーション系 | 1883 | 0 | 0 |

### ドキュメント化

件数は `docs/imp/imp-emoji-count-summary.md` に記録した。

このファイルには次も併記した。

- rank ごとの意味
- `99-bk` / `90-trush` の位置づけ
- `4_rank-d` が現状 `illust` / `illust-text` / `text` の3分割構造ではないこと
- 現時点で「使う候補」と「基本使わない・優先しない候補」の読み方

## 判断メモ

- `1-rank-a` / `2-rank-b` / `3-rank-c` は、実運用上「使う候補」として見てよい状態
- `4_rank-d` は不採用ではなく、アニメーション系として今回の本線分類から外しているだけ
- `99-bk` / `90-trush` は件数サマリ表には入れていないが、補足として意味を残している

## 補足

最初に作成した `imp-emoji-count-summary.md` の表は、Markdown の区切り行が1列足りず、レンダリング時に崩れていた。

そのため、表のヘッダ行を標準的な5列の Markdown 表へ修正した。

## 次に見ればよいファイル

- `docs/imp/imp-emoji-count-summary.md`
