# Emoji Count Summary

カスタム絵文字の分類先フォルダ件数サマリ。

**集計日**: 2026-05-10  
**集計対象**: `data/images`

## 集計結果

| rank | 意味 | illust | illust-text | text |
|---|---|---:|---:|---:|
| `1-rank-a` | ノート本文にもリアクションにも使いやすい候補 | 1271 | 230 | 1110 |
| `2-rank-b` | 主にノート本文向きの候補 | 298 | 188 | 1827 |
| `3-rank-c` | 主にリアクション向きの候補 | 90 | 45 | 401 |
| `4_rank-d` | 今回の主分類対象から外したアニメーション系 | 1883 | 0 | 0 |

## 補足

- `99-bk`
  - 感情表現用途としては弱く、情報ラベル寄りのものを置く想定。
- `90-trush`
  - 使いづらい、攻撃的、露骨、優先度が低いものを置く想定。
- `1-rank-a` / `2-rank-b` / `3-rank-c` は、各フォルダ直下に `illust` / `illust-text` / `text` があるため、その実ファイル数を集計している。
- `4_rank-d` は現状 `illust` / `illust-text` / `text` の3分割構造ではなく、元カテゴリ階層のまま格納されている。
- そのため `4_rank-d` については、実ファイル総数 `1883` を `illust` 欄へ記録し、`illust-text` / `text` は現行ツリー上では未分類として `0` 扱いにしている。

## 現時点の読み方

- 基本的に「使う候補」は `1-rank-a` / `2-rank-b` / `3-rank-c`
- 「今回は基本使わない・優先しない」は `4_rank-d` / `99-bk` / `90-trush`
- ただし `4_rank-d` は不採用ではなく、アニメーション系として今回の本線分類から外しているだけ
