# Consent Experience Strategy

## 目的

フォロワーの投稿を、キャラクターの疑似生活体験の参考にする。

ただし、勝手に引用・模倣・体験化すると不快感や誤解につながるため、明示的な同意を得たユーザーを優先する。

この仕様は「キャラクター自身が体験したことに変換する」場合の同意戦略である。

個人を特定せず、引用もせず、TL全体の観測として「何々をしてる人がいた、いいなー」と投稿する場合は、許可不要とする。ただし安全判定は必須。

## 基本戦略

1. ユーザーからフォローされる。
2. botがフォローお礼と許可依頼のリプライを送る（DM指定）。
3. 依頼ノートは可能ならリアクション受付を「❤のみ」にする。
4. ❤リアクションをもらったユーザーを許可済みとしてDBに保存する。
5. ホームタイムライン取得時、許可済みユーザーのnoteを優先して疑似体験候補にする。
6. **5分ごとの行動ガチャ**で、投稿するかどうか、何を投稿するかを決める。
7. TL観測が当選した場合、引用RNガチャに進む。
8. 引用RNが当選した場合、許可済みユーザーのnoteから安全な候補を選び、引用RN投稿する。
9. 投稿成功時のみ `experience_logs` に保存する。
10. 必要に応じて元ユーザーに言及し、引用Renoteを使う。

## 許可依頼文

```text
@<username> フォローありがと。
あなたの投稿を私の生活の一部にしてもいい？
よければ、ピン留めノートに❤をつけてね。
```

**実装状況**: `src/probe.ts` の `buildFollowGuide` で実装済み。visibility=specifiedでDM相当に送信。

## 体験投稿文

許可済みユーザーのnoteを体験候補として採用し、投稿時に体験として実行する場合の文案。

引用RNする場合:

```text
<引用コメント生成結果>

引用RN: <対象ノート>
```

**実装状況**: `src/ai/generate-quote-post.ts` で引用コメント生成を実装。実際の投稿は `src/scheduled-post.ts` の `generateAndPost` で実行。

## 探索ロジック（実装済み）

**5分ごとの行動ガチャ**（`src/scheduled-post.ts`）で実行する。

流れ:

1. ホームタイムラインからnoteを取得する（`src/tl-scan.ts`）。
2. CW・renote・空テキストを除外し、`source_notes` に保存する。
3. TL観測ガチャに当選した場合:
   - 引用RNガチャに進む
   - 許可済みユーザー（`experience_source_consents`）をランダムに5人取得
   - 各ユーザーの直近ノート（1週間以内）を20件取得
   - 構造フィルタ（CW/reply/renote/非公開除外）
   - 直近24時間のノートを優先
   - AI安全判定（`src/ai/classify-quote-safety.ts`）でOKなら採用
4. 候補が見つかった場合、引用コメントを生成し、引用RN投稿する
5. 投稿成功時のみ `experience_logs` に記録する

**実装詳細**: `src/quote-pick.ts` の `pickQuoteCandidate` 関数

## 採用してよいnote

- 公開またはホーム向けで、botが正当に閲覧できるnote。
- 体験候補にする場合は、許可済みユーザーのnote。
- TL観測にする場合は、許可済みユーザーに限定しない。
- センシティブ、政治、医療、投資、成人向け、個人情報、揉め事を含まないnote。
- 位置情報や生活圏が特定されにくいnote。
- botが引用しても不自然に迷惑にならないnote。

## 採用しないnote

- CW付きで中身の扱いが難しいnote。
- NSFW / sensitiveなnote。
- 個人情報、居場所、勤務先、学校、顔写真などに近い情報を含むnote。
- 悩み、病気、事故、トラブル、怒り、炎上、対人関係の深刻な話題。
- 他者へのリプライ文脈が強く、引用すると会話に割り込む形になるnote。
- 削除済み、非公開化、参照不能になったnote。

## 重要な安全弁

- TL観測では個人名を出さない。
- TL観測では引用Renoteしない。
- TL観測では元noteの文面を再利用しない。
- TL観測は複数noteから受けた雰囲気として表現する。
- プロフィールにbotであること、投稿を参考にする仕組み、オプトアウト方法を書く。
- ピン留めノートに、許可依頼・許可条件・対象外にする内容・取り消し方法を書く。
- 許可はユーザー単位で保存するが、note単位の採用判定も必ず行う。
- **1ユーザーを短期間に何度も引用しない**（実装: 直近24時間のノートを優先しつつ、ランダム性を持たせる）。
- **同じユーザーの参照は最低でも数日空ける**（実装: 1週間以上前のノートは対象外）。
- 引用Renoteを毎回使わない。引用なしの抽象化投稿も混ぜる（実装: 引用RNガチャで制御）。
- 「勝手に真似された」と感じさせない表現にする。
- 「拡散してあげる」ではなく、「見て楽しそうだったから影響を受けた」という温度にする。
- オプトアウト文言をプロフィールや定期案内に置く。
- `/stop` はリプライや引用RNなどの接触停止として扱う（実装済み）。
- `/unfollow` はbot側から実際にフォロー解除し、ノート参照対象からも除外する（実装済み）。
- 候補として拾っただけの内容を、後から「前に体験したこと」として扱わない。
- 「体験した」と扱うのは、その内容をノートとして投稿したものだけにする（`experience_logs` に投稿成功時のみ保存）。

## プロフィール記載案

```text
botです。❤で許可してくれた人の公開ノートを、私の行動ログや日記のヒントにすることがあります。
引用RNやリプライを止めたい時は `/stop`、フォロー解除と参照停止は `/unfollow` とリプライしてください。
```

**実装状況**: プロフィール文として手動設定が必要。

## ピン留めノート案

```text
このアカウントはbotです。
このノートに❤をくれた人の公開ノートを、私の行動ログや日記のヒントにすることがあります。

やること:
- たまに投稿を引用RNします。（個人情報やセンシティブな話題は拾いません）
- たまにリプライを飛ばします。
- 使う時は、私が見て影響を受けた体験として書きます。
- 投稿をそのままコピーするのではなく、私の言葉に抽象化します。

やめたい時:
- リプライや引用RNをやめてほしいとき：「/stop」とリプライしてください
- フォロー解除とノート参照停止：「/unfollow」とリプライしてください

`/unfollow` を受け取った場合、このbotはあなたのフォローを外し、以後あなたのノートを体験候補に使いません。
```

**実装状況**: ピン留めノートは手動作成。note idを環境変数で設定する必要がある。

## リアクション受付について

Misskeyのリアクション機能には、投稿時に受け付けるリアクション種別を制限する機能がある。

公式ドキュメント上では「Likes only」により❤のみ受け付けられる。misskey.io APIのnoteレスポンスにも `reactionAcceptance` が存在する。

実装時は、`notes/create` のリクエストで設定できる具体的な値を実機またはOpenAPI定義で確認する。現時点では仕様上の意図として「❤のみ」を採用し、実装値は未確定とする。

**実装状況**: `src/probe.ts` の `acceptedHeartReactions` で❤と❤️を受け付けるように実装済み。

## 成長戦略としての見立て

フォローするとキャラクターの行動に影響でき、場合によって引用される、という体験はフォロー動機になり得る。

ただし、拡散目的が前に出るとbotの印象が悪くなる。主軸は「許可した人の投稿から、キャラクターが生活感を得る」ことに置く。

## 実装ファイル対応表

| 仕様上の機能 | 実装ファイル | 状態 |
|---|---|---|
| フォロー通知検知 | `src/probe.ts` `handleFollowNotification` | 実装済み |
| フォローお礼DM送信 | `src/probe.ts` `buildFollowGuide` | 実装済み |
| ❤リアクション検知 | `src/probe.ts` `handleConsentReactions` | 実装済み |
| 許可済みユーザー保存 | `src/probe.ts` `saveConsentReaction` | 実装済み |
| ホームTL取得 | `src/tl-scan.ts` `runTlScan` | 実装済み |
| 行動ガチャ | `src/scheduled-post.ts` `drawAction` | 実装済み |
| 引用候補選定 | `src/quote-pick.ts` `pickQuoteCandidate` | 実装済み |
| AI安全判定 | `src/ai/classify-quote-safety.ts` | 実装済み |
| 引用コメント生成 | `src/ai/generate-quote-post.ts` | 実装済み |
| 体験ログ保存 | `src/scheduled-post.ts` `generateAndPost` | 実装済み |
| `/stop` コマンド | `src/probe.ts` `handleReplyCommand` | 実装済み |
| `/unfollow` コマンド | `src/probe.ts` `handleReplyCommand` | 実装済み |

## 参考リンク

- Misskey Reactions: https://misskey-hub.net/en/docs/for-users/features/reaction/
- misskey.io `notes/create`: https://api-doc.misskey.io/api-7254018
- misskey.io API docs: https://api-doc.misskey.io/
